package algorithms;

/**
 * Heap Sort implementation (Member 2's task).
 */
public class HeapSort {
    public static <E extends Comparable<E>> void sort(E[] array) {
        // Implementation will be added by Member 2 in their feature branch
    }
}